public class StringClass {
    public static void main(String args[]){
        String name = "Sam";
        System.out.println("My name is: " + name);
        System.out.println("I love soccer");

    }
}
    //String is an object typically representing a sequence of characters
//java.lang.String is used to create String object
//String are always in double quotes
    //DATATYPES vvv
        //boolean result = name.equals("Haily"); //case sensitive unless using name.equalsIgnoreCase();
        //boolean result = name.isEmpty(); //true if empty, false if not empty
        //int result = name.length(); //will return value of characters in String Haily=5
        //int result = name.indexOf("a"); //will return the number of the character space a=1
        //char result = name.charAt(0); //will return the character of the number space 0=H
        //String result = name.toUpperCase(); //will make name uppercase HAILY //toLowerCase haily
        //String result = name.trim //will trim any extra spaces
